# Core runtime dependencies for your pipeline scripts
pandas>=2.0.0,<3.0.0
numpy>=1.23.0,<1.25.0
openpyxl==3.1.2
psycopg2-binary>=2.9.9          # PostgreSQL driver for db connections
python-dotenv>=1.0.0            # For load_dotenv()
pyyaml>=6.0.0                   # For yaml.safe_load()
joblib>=1.3.0                   # For loading .pkl models (XGBoost, ElasticNet, Prophet)

# Model-specific 
prophet>=1.1.2                  # Facebook Prophet for monthly forecasting
xgboost>=1.7.0                  # XGBoost models 
scikit-learn>=1.2.0,<1.4.0      # ElasticNet from sklearn.linear_model 